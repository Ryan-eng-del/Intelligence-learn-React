import styled from 'styled-components'

export const PublishTypeWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 10px;
  .bl {
    padding-right: 5px;
  }
  .highset {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
`
