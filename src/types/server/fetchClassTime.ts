export interface RelatePointsResource {
  related_points: string[]
  course_id: string
  file: any
}
export interface AddContentResource {
  related_points: string[]
  course_id: string
  file: FormData
}
